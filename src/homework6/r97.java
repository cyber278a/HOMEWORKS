package homework6;

import java.util.Scanner;

public class r97 {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the browser name to proceed further with execution");
        String browser = sc.nextLine();
        String newStr=browser.substring(0,1).toUpperCase()+browser.substring(1).toLowerCase();
        System.out.println("Proceed with " +newStr+ " browser");

// changing any word so it will start with upper case
    }
    }

